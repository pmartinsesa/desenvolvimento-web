<%= form_with(model: products_storage) do |form| %>
  <% if products_storage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(products_storage.errors.count, "error") %> prohibited this products_storage from being saved:</h2>

      <ul>
        <% products_storage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :storage_id, style: "display: block" %>
    <%= form.text_field :storage_id %>
  </div>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
